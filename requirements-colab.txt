# AnimeGANv3 inference-only dependencies for Google Colab (T4 / CUDA 12)
# Keep the baseline small: no TensorFlow training stack is required.
numpy>=1.26,<3
Pillow>=10,<12
tqdm>=4.66
opencv-python-headless>=4.9
onnxruntime-gpu>=1.18
